SECRET_KEY=django-insecure-change-me-in-production
DEBUG=True
ALLOWED_HOSTS=localhost,127.0.0.1,web,0.0.0.0

DB_ENGINE=django.db.backends.postgresql
DB_NAME=supportai
DB_USER=postgres
DB_PASSWORD=postgres
DB_HOST=localhost
DB_PORT=5433

REDIS_URL=redis://127.0.0.1:6380/0
SEED_DEMO_DATA=false

CORS_ALLOWED_ORIGINS=http://localhost:5173,http://127.0.0.1:5173

JWT_ACCESS_MINUTES=60
JWT_REFRESH_DAYS=7

MEDIA_ROOT=media_uploads
MEDIA_URL=/media/

# OpenRouter
OPENROUTER_API_KEY=sk-or-v1-your-key-here
OPENROUTER_MODEL=liquid/lfm-2.5-2.6b:free

# Vector RAG (pgvector cosine search in Postgres)
RAG_TOP_K=4
RAG_MIN_SCORE=0.32
RAG_EMBED_DIM=384

# Google OAuth (same Client ID as frontend VITE_GOOGLE_CLIENT_ID)
GOOGLE_CLIENT_ID=your-client-id.apps.googleusercontent.com

# Password reset emails (link opens this frontend)
FRONTEND_URL=http://localhost:5173

# Email — console for local print; SMTP for real delivery (Gmail App Password)
# EMAIL_BACKEND=django.core.mail.backends.console.EmailBackend
EMAIL_BACKEND=django.core.mail.backends.smtp.EmailBackend
EMAIL_HOST=smtp.gmail.com
EMAIL_PORT=587
EMAIL_HOST_USER=your-gmail@gmail.com
EMAIL_HOST_PASSWORD=your-16-char-app-password
EMAIL_USE_TLS=True
DEFAULT_FROM_EMAIL=your-gmail@gmail.com
